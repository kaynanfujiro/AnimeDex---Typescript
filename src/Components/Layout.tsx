

export const Layout = ({children}: any) => {
    return(
        <div className="bg-gray-500 min-h-screen w-full m-0 p-0 flex justify-center items-center">
            {children}
        </div>
    )
}